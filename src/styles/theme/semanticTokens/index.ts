import { SemanticTokens } from "@pandacss/dev";

import { colors } from "./colors";
import { shadows } from "./shadows";

const semanticTokens: SemanticTokens = { colors, shadows };

export default semanticTokens;
