import { css } from "@/styled-system/css";

export const buttonGroup = css({
  "& > hr": {
    width: "1px",
    borderWidth: 0,
    backgroundColor: "currentColor",
    height: "1.25rem",
    marginLeft: "0.5rem",
    marginRight: "0.5rem",
  },
});
